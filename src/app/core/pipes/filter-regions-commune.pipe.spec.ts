import { FilterRegionsCommunePipe } from './filter-regions-commune.pipe';

describe('FilterRegionsCommunePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRegionsCommunePipe();
    expect(pipe).toBeTruthy();
  });
});
